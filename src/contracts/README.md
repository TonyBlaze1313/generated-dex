# Contract Integration

- Each network (arcTestnet, robinhoodTestnet, hardhat, etc.) has its own addresses.json file.
- Use these files to dynamically connect your frontend to the correct contracts based on the selected network.
- ABIs can be added here as needed for contract interaction.
